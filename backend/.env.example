# Environment Configuration
# Copy this file to .env and update the values

# Server Configuration
PORT=5001

# Environment (development | production)
# In production, console.log/info/debug will be disabled
NODE_ENV=development

# Database Configuration
# DB_MODE=sqlite (default) or json
# If using SQLite, data will be stored in stores.db
# If using JSON, data will be stored in stores.json
DB_MODE=sqlite
